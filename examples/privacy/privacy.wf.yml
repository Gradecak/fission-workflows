apiVersion: 1
output: Privacy
tasks:
  Privacy:
    run: hello
    inputs:
      headers:
        X-CustomFn: "tr"
      query:
        runtime: "3"
    # execOpts:
    #   multizone: true

dataflow:
  consentCheck: true
